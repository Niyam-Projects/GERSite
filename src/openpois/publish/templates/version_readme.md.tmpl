# OpenPOIs {version_folder}

Snapshot generated on {generation_date}. See the
[top-level README](../README.md) for dataset documentation, quickstart code,
and license.

## Source data

These three values are what a consumer needs to reproduce this release.

| Component | Version |
|---|---|
| OSM snapshot | `{osm_snapshot_date}` (Geofabrik download date) |
| Overture release | [`{overture_release}`](https://docs.overturemaps.org/release-calendar/) |
| Turnover model | [`{model_commit}`](https://github.com/henryspatialanalysis/openpois/commits/{model_commit}) |

## Row counts

| Dataset | Rows |
|---|---|
| `osm-parquet/` | {osm_row_count} |
| `conflated-parquet/` | {conflated_row_count} |

## Files

```
{version_folder}/
├── README.md                         # this file
├── osm-parquet/                      # OSM-only snapshot, hive-partitioned by geohash prefix
├── osm-pmtiles/osm.pmtiles           # OSM snapshot as a single PMTiles archive (z14)
├── conflated-parquet/                # OSM × Overture conflation, hive-partitioned by geohash prefix
└── conflated-pmtiles/conflated.pmtiles
```

## Reproducing this build

Config fingerprint: `{config_hash}` (SHA-256 of the `config.yaml` that
produced this version; available alongside the data in the openpois source
repo).
